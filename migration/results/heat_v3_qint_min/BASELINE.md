# v3 事实基线卡（A0）

> 冻结日期：2026-08-03（M1）
> 地位：后续所有消融（M3/A1–A4）、升级（G7/v4）与统计扩展（M4/20 批）的
> **唯一可比基线**。任何报告引用 v3 数值时以本卡为准，禁止手工重复抄写。
> 唯一事实源文档：`migration/reports/迁移后续工作/COMSOL场景v3唯一可行配置与通过机制.md`

---

## 1. 基线身份

| 项 | 值 |
|---|---|
| 配置文件 | `migration/configs/heat_v3_qint_min.yaml`（已锁定，见文件头 LOCKED 注记） |
| 配置指纹 | sha256 `c4d665e485b18fa1e3fb5e47e6ecd7c6164cace8b4be0ea601226d79abd75300` |
| 特征变体 | `heat_v3_qint`（no_qq 6 列 + `q_int·P^(−0.7)` + `q_int/(1+hL/k_nom)·P^(−0.7)`） |
| H 库 | 11 列 4 组（θ=1/k 仿射；有效秩 7） |
| 弱标签 | refresh 模式（随当前 θ 逐轮 BCD 重估低保真闭式解） |
| 选参规则 | `minimum`（验证集最小 MSE；热传导场景规则，非全局默认） |
| lambda 网格 | physics [0.01, 0.1, 1, 10] × l1 [0.001, 0.01, 0.1] × group [0.001, 0.01, 0.1]（36 候选） |
| 其他关键参数 | `d_ridge=10`、`admm_max_iter=20000`、`bcd_max_iter=80`、`require_convergence=true`、`train_fraction=0.7`、`refit_on_train_validation=false` |
| 实验种子 | 2026（逐重复种子由协议派生） |
| 批次 | 1–10（每批一个 k_true，见 §3） |
| 标签档 | 10, 20, …, 120（12 档，嵌套 70/30 训练/验证划分） |
| 基线模型 | Ridge, SVR, DT, RF, GBDT, GP, Physics（标定+GP 残差）, PhysicsDirect |
| 结果目录 | `migration/results/heat_v3_qint_min/`（**只读冻结，任何复核写入新目录**） |

## 2. 环境与依赖锁

| 项 | 值 |
|---|---|
| 包版本 | pwl-repro 0.1.0（`pwl_repro` + `pwl_migration` 双包） |
| Python | 3.11.15（Windows-10-10.0.26200-SP0，见 `metadata.json`） |
| 依赖锁 | 根级 `uv.lock`，sha256（前 16 位）`ac575996e406e807` |
| 运行元数据 | `metadata.json`（本目录，含完整配置快照） |

## 3. 数据版本记录

数据：`migration/datasets/batch_01..10`（规格 v1.3；σ = 4.494 K，验收集 MSE
下限 σ² ≈ 20.2 K²）。每批 3 个 CSV 的 sha256 记录于各批 `metadata.json`；
**2026-08-03 M1 复核：30/30 文件实际哈希与元数据记录一致**。

| 批次 | k_true | 批次 | k_true |
|---|---:|---|---:|
| 01 | 18.239 | 06 | 28.225 |
| 02 | 23.487 | 07 | 34.507 |
| 03 | 22.096 | 08 | 16.157 |
| 04 | 16.475 | 09 | 32.974 |
| 05 | 27.188 | 10 | 15.096 |

数据层验收：`migration/datasets/acceptance_report.json`（全部通过；
sha256 前 16 位 `193a04e82b085254`）。

## 4. 基线指标（10 批均值测试 RMSE，K）

| 模型 | @10 | @120 |
|---|---:|---:|
| **PWL** | **8.055** | **5.366** |
| Physics（标定+GP） | 9.585 | 5.530 |
| GP | 10.954 | 5.673 |
| GBDT | 11.087 | 6.330 |
| Ridge | 10.056 | 6.386 |
| RF | 10.948 | 6.414 |
| SVR | 12.892 | 7.832 |
| DT | 13.388 | 8.364 |
| PhysicsDirect | 15.668 | 15.668 |

- PWL @120 MSE = 28.85 K²，MSE/σ² = 1.428（迄今最接近噪声下限）；
- **12 个标签档 PWL 均值全部最优**（自 n=90 起逐批全部最优）；
- 小样本优势：gap@10 = 2.000 ≥ gap@120 = 0.307；
- 无噪声类偏置 δ² ≈ 0.44 K²（两列机理列修复后，归因复算口径）。

## 5. 统计口径（配对单侧 t 检验，10 对，rmse）

| 对照 | @120 均值差 | p | Holm 校正 |
|---|---:|---:|---:|
| PWL < Physics | −0.163 | 0.207 | 1.000 |
| PWL < GP | −0.307 | 0.068 | 1.000 |
| PWL < GBDT | −0.964 | 1.2e-5 | 0.0024 |

**结论口径：均值通过锚点 6，统计不显著**；显著性收紧通道为批次扩展（M4，10→20 批）。

## 6. 锚点状态（`quality_checks.csv`）

pass：有限性、收敛率 1.0、无效候选率 0.2435（≤0.25，贴限）、端点下降、
小样本优势、MSE/σ²@120 = 1.428、优于 PhysicsDirect、**优于 Physics（均值）**；
info：d 系数物理一致性 1.0；
fail：双保真相关性 [0.691, 0.854]（数据属性）、θ 学习 0.3（结构固有，M2 修订判据）；
warn：θ 撞界率 0.6。

## 7. 复核记录（M1，2026-08-03）

- 复核运行：`migration/results/heat_v3_qint_min_recheck/`（同配置、同种子、同数据，
  独立输出目录；命令见 `migration/README.md`）。
- 复核结论：**无未解释的实质漂移**——`quality_checks.csv` 与 `conditions.csv`
  完全一致；`results.csv` 1080 行中 1079 行逐值一致，唯一差异为 1 行 RF 基线
  （repeat 1, @110）RMSE 7.588660 vs 7.588653（≈1e-5 相对抖动，sklearn
  随机森林并行浮点非确定性），不影响任何 PWL 结果与结论；
  逐批配对统计口径复算一致（§5）。
- 原始目录未被修改或覆盖。

## 8. 追溯规则

1. 后续实验必须以新配置 + 新结果目录进行，并在报告中与本卡逐项对照；
2. 报告引用 v3 数值时引用本卡（§4/§5/§6），禁止手抄；
3. 若数据、配置或代码发生任何变更，须新起基线卡并记录变更原因；
4. 本目录（`heat_v3_qint_min/`）只读；复核、重跑一律写入 `*_recheck` 等新目录。
