# 加热配方计算中代理模型的必备条件

> 版本：v1.0　日期：2026-08-04
> 定位：回答"在加热配方（最优控制/MPC）计算中，一个代理模型（Surrogate Model）必须满足哪些条件才可用"这一问题。

---

## 0. 背景：代理模型处在什么位置

加热配方优化的完整链路为：

```
热传导 PDE（物理真身）
   → 状态空间模型  x(k+1) = F(x(k), u(k), d(k))
      → 最优控制 / MPC 求解器（调用成千上万次正向仿真）
```

正向仿真是求解器的"计价器"，每评估一条候选配方就要完整滚动一次。
当 PDE/高保真仿真太慢时，**代理模型替代状态方程**为求解器服务。
因此代理模型的"必备条件"不是由建模偏好决定的，而是由它服务的对象——
**优化器 + MPC 闭环**——的刚性需求反推出来的。

---

## 1. 必备条件总览

| # | 条件 | 类别 | 缺失后果 |
|---|------|------|---------|
| 1 | 接受"当前状态"作为输入 | 结构 | 退化为开环回放器，无法闭环 |
| 2 | 具备时间推进（动态）机制 | 结构 | 无法滚动预测轨迹 |
| 3 | 多步滚动的误差可控 | 精度 | 轨迹失真，配方不可信 |
| 4 | 对约束相关量的精度优先保证 | 精度 | 安全约束被隐性违反 |
| 5 | 单次评估足够快 | 算力 | 无法供优化器/MPC 高频调用 |
| 6 | 输出对输入平滑（可微更佳） | 接口 | 梯度类求解器失效或震荡 |
| 7 | 外推行为受控且可诊断 | 鲁棒 | 优化器出包络后得到荒谬配方 |
| 8 | 提供不确定性量化 | 鲁棒 | 无法做安全裕度（backoff）设计 |
| 9 | 支持在线校正/更新 | 寿命 | 炉况漂移后精度持续退化 |
| 10 | 与优化问题的数学结构兼容 | 接口 | 无法嵌入 MPC/递阶体系 |

---

## 2. 结构类必备条件（缺一不可）

### 2.1 必须接受当前状态输入

代理模型必须形如：

$$\hat{x}_{k+1} = f_{\theta}(x_k, u_k, d_k)$$

- $x_k$：当前状态（钢坯节点温度分布或其降阶系数）
- $u_k$：控制输入（各段炉温设定值——即"配方"的数学实体）
- $d_k$：外生不可控输入（钢种、规格、入炉温度、节奏）

**判别测试**：输入同一时刻 $t$、但两组不同的当前温度分布，
模型必须给出不同的输出。若做不到，它只是 $T = f(t)$ 型开环轨迹回放器，
一旦入炉温度波动或轧线节奏变化，预测系统性失真且无任何自修正机制。

### 2.2 必须具备动态推进机制

模型必须能"走一步看一步"：以上一步输出作为下一步输入，递归展开整条温度历程。
静态映射（如原生克里金 $y = f(\mathbf{z})$）不满足此条，
必须改造为**单步转移代理**（学习 $x_k \to x_{k+1}$ 的映射）后方可使用。

---

## 3. 精度类必备条件

### 3.1 多步滚动误差可控（而非单步精度高）

配方评估需要连续调用模型 $K$ 步（$K$ 可达数百），单步误差 $\epsilon$ 会逐层累积。
因此验收指标不是单步 RMSE，而是：

$$\text{Rollout Error}(H) = \frac{1}{H}\sum_{k=1}^{H} \|\hat{x}_k - x_k^{\text{真}} \|$$

必须实测不同预测时域 $H$ 下的滚动误差曲线，确认在 MPC 预测时域内误差不发散。
注意：单步精度优秀但滚动发散的模型（常见于自回归结构）**不可用**。

### 3.2 约束相关量精度优先

优化问题中的约束集中在特定物理量上：

- 表面温度 $T_{\text{表}}$（上限约束，防过烧/氧化）
- 断面温差 $T_{\text{表}} - T_{\text{中心}}$（安全约束，防裂纹）
- 出炉温度 $T_K$（目标命中）

代理模型在这些量上的偏差会直接转化为**隐性违规或虚假达标**。
要求：对约束相关输出做专项精度验证，允许整体平均误差稍高，
但约束量的误差必须小于安全裕度 $\delta$ 的一半。

---

## 4. 算力类必备条件

### 4.1 单次评估速度的硬指标

设 MPC 控制周期为 $T_c$（秒级~分钟级），优化器每次求解调用模型 $N_{\text{eval}}$ 次
（通常 $10^3 \sim 10^5$），则：

$$t_{\text{单次}} \le \frac{T_c}{N_{\text{eval}} \times \text{安全系数}}$$

工程经验：单次全轨迹滚动评估应控制在 **毫秒级**。
这是代理模型存在的根本理由——若代理模型不比 PDE 快 2~3 个数量级，它没有存在价值。

---

## 5. 接口类必备条件

### 5.1 输出对输入平滑，最好可微

主流优化器（SQP、内点法）依赖梯度。代理模型应满足：

- 至少 $C^0$ 连续（无跳变、无平台区）
- 理想情况 $C^1$ 可微，能提供 $\partial \hat{x}_{k+1} / \partial u_k$ 解析或自动微分梯度

反面教材：决策树/随机森林类模型输出分段常数，梯度为零或不存在，
嵌入梯度优化器会导致搜索停滞——这类模型只适合配无梯度算法（遗传算法、贝叶斯优化）。

### 5.2 与优化体系结构兼容

- 可嵌入 MPC 的滚动求解循环（状态可注入、可重置）
- 可嵌入递阶结构：为 SPC 层提供设定值寻优接口，响应时间匹配层间周期
- 输入维度与优化变量定义一致（或提供参数化配方的低维接口）

---

## 6. 鲁棒类必备条件

### 6.1 外推行为受控且可诊断

优化的本质是探索训练数据包络之外的操作点——**代理模型必然被用到外推区域**。
要求：

1. 外推时预测值退化要"优雅"（朝均值回归而非发散到荒谬值）
2. 必须能给出"此查询点是否在训练包络内"的诊断
   （如克里金的预测方差、输入空间的凸包/距离度量）
3. 优化流程中应设置包络约束或信任域，阻止求解器进入诊断为不可信的区域

### 6.2 不确定性量化（UQ）

必须输出预测置信度，用途：

- 约束安全裕度设计：$T_{\text{表}} \le T_{\max} - \delta$，其中 $\delta = k\cdot\sigma_{\text{预测}}$
- 防止"贴边界运行"（bang-bang 解的固有倾向）在模型误差下变成真实违规
- 贝叶斯优化类算法（EGO）以预测均值+方差为驱动，无 UQ 则算法不成立

克里金/高斯过程在此项有天然优势；神经网络需补 MC-Dropout、深度集成等手段。

---

## 7. 寿命类必备条件

### 7.1 支持在线校正

炉子不是定常系统：积灰、衬里老化、传感器漂移使真实 $F$ 缓慢变化。
代理模型必须具备以下至少一种机制：

- **偏差校正项**：模型输出 + 在线辨识的残差项（工程量最小，首选）
- **参数自适应**：$\theta$ 随新数据滚动更新（递推最小二乘/Kalman 框架）
- **定期重构**：离线重训练 + 版本化上线（需配套验证流程）

判别问题：**"上线六个月后，模型靠什么维持精度？"** 没有答案的模型不能上线。

---

## 8. 验收清单（上线前逐项打勾）

```
□ 结构：输入包含 (x_k, u_k, d_k)，非纯时间函数
□ 结构：支持多步递归滚动，状态可注入/重置
□ 精度：MPC 预测时域内滚动误差不发散（附实测曲线）
□ 精度：表面温度、断面温差、出炉温度专项误差 < δ/2
□ 算力：单条全轨迹评估 < 毫秒级
□ 接口：输出 C0 连续，可微性明确，与求解器匹配
□ 接口：可嵌入 MPC 循环与递阶控制结构
□ 鲁棒：外推诊断机制存在，优化流程含信任域/包络约束
□ 鲁棒：提供预测方差，安全裕度设计已完成
□ 寿命：在线校正/更新机制明确，含精度退化监控
```

---

## 9. 常见误区的三个提醒

1. **"离线 R² 高 = 可用"是最大误区。** 离线拟合优度不覆盖滚动误差、
   外推行为、约束量精度这三项真正决定成败的指标。
2. **代理模型不消除对机理的需求。** 状态量的定义、约束的物理形式、
   外推风险的判断，全部来自对热传导 PDE 的理解——代理模型是机理的加速器，
   不是替代品。
3. **必备条件随用途分层。** 仅做离线配方寻优（贝叶斯优化），
   条件 1/2 可放宽为静态响应面；一旦进入在线 MPC 闭环，
   十条全部生效，无豁免项。

---

## 10. 分路线实施文档

- [路线 A：离线加热配方代理模型实施方案](migration/reports/加热配方代理模型/路线A_离线加热配方代理模型实施方案.md)：
  参数化配方、多指标静态 PWL、约束寻优、UQ、信赖域和高保真回查闭环。
- [路线 B：在线 MPC 动态 PWL 实施方案](migration/reports/加热配方代理模型/路线B_在线MPC动态PWL实施方案.md)：
  状态降阶、动态多输出 PWL、rollout、MPC 接口、不确定性传播、在线校正和安全回退。
- [路线 C：PWL–Kriging 混合代理模型方案](migration/reports/加热配方代理模型/路线C_PWL-Kriging混合代理模型方案.md)：
  PWL 均值通道 + 克里金式方差通道的混合形态，残差 GP / Bootstrap-保形两臂、覆盖率校准、
  混淆监测与包络诊断，首期作为路线 A 的 UQ 环节在热传导场景验证。

---

*本文档为加热配方优化系统工程系列文档之一，聚焦代理模型准入标准。*
