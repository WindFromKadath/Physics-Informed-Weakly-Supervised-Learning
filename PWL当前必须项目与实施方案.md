# PWL 当前必须项目与实施方案

> 日期：2026-08-04
> 状态：执行计划 v1.1；M0–M4 已完成，M5 已完成本地 Git 索引收口
> 定位：将当前阶段的必要收口工作转换为可执行任务、交付物和验收门禁
> 上游文档：[阶段性总结](阶段性总结_PWL算法实现框架.md)、
> [v3 通过后下一步计划](migration/reports/迁移后续工作/COMSOL场景v3通过后下一步计划.md)、
> [v3 唯一可行配置](migration/reports/迁移后续工作/COMSOL场景v3唯一可行配置与通过机制.md)

---

## 0. 执行结论

当前阶段的首要目标不是继续扩充算法，而是把热传导 v3 从“10 批均值上有效的
实验配置”收口为“机制清楚、统计口径明确、能够复现并可对外说明的框架实例”。

必须完成的工作共六项：

1. 统一文档与结论口径；
2. 冻结并登记 v3 事实基线；
3. 修订 `theta` 的解释与验收标准；
4. 完成核心机制消融；
5. 扩展至 20 批并完成统计闭环；
6. 建立一键复现与 GitHub 交付入口。

以上六项闭合前，refit、active-set debias、变量投影、rank-7 `H`、样本级物理
权重等内容只作为 v4 研究候选，不并入当前主配置。

---

## 1. 当前事实基线

唯一事实基线为：

| 项 | 当前值 |
|---|---|
| 配置 | `migration/configs/heat_v3_qint_min.yaml` |
| 特征变体 | `heat_v3_qint` |
| 弱标签 | 随 `theta` 刷新 |
| 选参规则 | `minimum`，仅作为热传导场景规则 |
| 批次 | 1–10 |
| 标签档 | 10–120，共 12 档 |
| 结果目录 | `migration/results/heat_v3_qint_min/` |
| PWL @120 | RMSE 5.366，MSE 28.85 K² |
| Physics-GP @120 | RMSE 5.530 |
| GP @120 | RMSE 5.673 |
| 噪声方差 | 约 20.2 K² |
| 类偏置 `delta^2` | 两列修复后约 0.44 K² |
| 统计结论 | 10 批均值领先，配对检验尚不显著 |

当前结果目录不得被后续试验覆盖。任何复核运行必须写入新目录，并保留配置、环境、
逐批结果和质量检查文件。

---

## 2. 必须项目总表

| 编号 | 必须项目 | 当前状态 | 依赖 | 终态交付物 |
|---|---|---|---|---|
| M0 | 文档与结论统一 | ✅ 已完成（2026-08-03） | 无 | 主报告 §0 回写、阶段总结、计划和 README 口径一致；113 链接校验通过 |
| M1 | v3 基线冻结 | ✅ 已完成（2026-08-03） | 无 | `results/heat_v3_qint_min/BASELINE.md`、配置锁定、复核无实质漂移、数据哈希 30/30 |
| M2 | `theta` 判据修订 | ✅ 已完成（2026-08-03） | M1 | theta=nuisance 定案；锚点检查 info 级 + 2 项新指标；《theta判据修订》 |
| M3 | 核心机制消融 | ✅ 已完成（2026-08-03） | M1 | 《v3机制消融报告》：无 B @120 +2.68 K、无弱标签 @10 +2.30 K、去 qint 锚点 6 翻转、n_weak≈100 饱和 |
| M4 | 20 批统计闭环 | ✅ 已完成（2026-08-03） | M1、外部数据 | `results/heat_v3_qint_min_b20/`、《20批统计闭环报告》：均值优势保持、Holm 0.071 未过校正门 |
| M5 | 一键复现与交付检查 | 🟡 部分完成：本地索引已按最终目录统一暂存 | M0–M4 | README、环境锁、CI/smoke 已具备；待 commit/push 与远端 CI |

---

## 3. M0：统一文档与结论口径

### 3.1 目标

消除“锚点 6 失败/通过”“P3 待办/完成”“minimum 是否全局推广”等版本冲突，
并统一“算法内核已实现”与“PWL 是场景化框架”的表述。

### 3.2 实施动作

1. 以 `migration/reports/迁移后续工作/COMSOL场景v3唯一可行配置与通过机制.md`
   作为 v3 配置事实源；
2. 将主迁移报告中的锚点 6 更新为“10 批均值通过、统计不显著”；
3. 将 P3 更新为“已完成：仿真保留 1-SE，热传导保留 minimum”；
4. 将 `theta` 的旧验收文字改为“等效热阻/预测稳定性”，并指向 M2；
5. 检查所有非 legacy 当前报告中的旧路径、旧模块名和旧结果目录；
6. 在 README 中明确：`legacy/` 只用于历史溯源，不能作为当前运行依赖。

### 3.3 验收标准

- 当前层级报告中不存在相互矛盾的“锚点 6 失败/通过”表述；
- P3 状态全部为已完成；
- minimum 明确标注为热传导场景规则，不是全局默认；
- “论文算法完整复现”被收紧为“公开数学结构与优化骨架已实现”；
- Markdown 相对链接有效，历史材料均从 `legacy/README.md` 进入。

---

## 4. M1：冻结并登记 v3 事实基线

### 4.1 目标

建立后续所有消融和升级唯一可比较的 A0 基线，防止配置漂移、数据变化或结果覆盖
被误认为算法收益。

### 4.2 实施动作

1. 锁定 `heat_v3_qint_min.yaml`，后续变体只通过新配置文件实现；
2. 记录数据批次 1–10 的文件清单、大小、更新时间和可复核摘要；
3. 在结果目录增加基线说明，登记种子、依赖锁、特征列、批次、标签档和选参规则；
4. 核对 `results.csv`、`summary.csv`、`quality_checks.csv`、统计检验和运行元数据；
5. 用新输出目录执行一次复核，不覆盖原结果：

```powershell
uv run python migration/run_migration.py `
  --config migration/configs/heat_v3_qint_min.yaml `
  --output migration/results/heat_v3_qint_min_recheck
```

### 4.3 验收标准

- 配置、数据和结果之间能够一一追溯；
- 同种子、同数据的复核结果无未解释的实质漂移；
- 12 个标签档、@120 指标和质量检查与事实基线一致；
- 原始 `heat_v3_qint_min/` 目录未被修改或覆盖；
- 后续报告统一引用基线卡，而不是手工重复抄写数值。

---

## 5. M2：修订 `theta` 的解释与验收标准

### 5.1 问题

热传导场景中 `H` 为 11 列但有效秩约 7，材料热导率与接触热阻又串联进入输出。
当前数据只能稳定约束总热阻，不能保证将 `theta=1/k` 恢复为真实材料参数。

### 5.2 实施动作

1. 保留现有 `theta` 预测通道，避免在收口阶段改变 v3 主模型；
2. 将解释改为以下二选一，并在 M2 实验后定案：
   - `theta_eff`：等效热阻或等效传热参数；
   - nuisance parameter：只承担弱标签校准与预测辅助作用；
3. 在质量检查中同时报告边界命中率、批间方差、学习分数和预测敏感度；
4. 用冻结/刷新现有结果量化 `theta` 更新的净预测贡献；
5. 修改迁移分析、消融报告、阶段总结和主报告中的锚点 4 文案。

### 5.3 验收标准

- 不再将“接近真实 `k`”作为唯一成功标准；
- 新判据能够解释 v3 的边界命中而不否定其预测结果；
- 代码质量检查与文档判据一致；
- 对外材料明确区分预测参数与可物理解释参数。

---

## 6. M3：完成核心机制消融

### 6.1 实验矩阵

所有实验必须复用 v3 的批次、标签档、种子、基线模型和 minimum 规则，只改变被
消融因素。

| 变体 | 建议配置名 | 唯一改动 | 回答的问题 |
|---|---|---|---|
| A0 当前 v3 | `heat_v3_qint_min.yaml` | 无 | 事实基线 |
| A1 无 B | `heat_v3_ablate_no_b.yaml` | B 仅保留截距或完全关闭差异项 | 差异补偿是否必要 |
| A2 无弱标签 | `heat_v3_ablate_no_weak.yaml` | `lambda_grid.physics=[0]` | 小样本优势是否来自物理知识 |
| A3 去 qint 两列 | `heat_v3_ablate_no_qint.yaml` | 回到 `heat_no_qq` | v3 大样本修复是否由机理列产生 |
| A4 弱标签量 | `heat_v3_nweak_{50,100,200}.yaml` | 仅改变 `n_weak` | 物理样本收益何时饱和 |

### 6.2 实施动作

1. 为无 B/无 qint 创建显式 FeatureSpec，禁止在运行时临时删除数组列；
2. 为每个变体增加列数、仿射性、可计算性和无泄漏测试；
3. 先跑 2 批 smoke，再执行 10 批完整实验；
4. 比较 @10、@120、MSE/噪声、选中 `lambda_physics`、无效候选率和 `theta` 行为；
5. 结果写入 `migration/results/heat_v3_ablate_*/`，不得复用主基线目录；
6. 无论是否符合预期，都按结果修订机制叙事，禁止只保留正向消融。

### 6.3 验收标准

- A1–A4 均有独立配置、测试、结果目录和报告表；
- 至少能区分物理弱标签、差异补偿和新增机理列的独立贡献；
- @10 与 @120 的作用机制分别说明；
- 弱标签数量给出收益拐点，或如实报告未观察到拐点；
- 消融结论被写回阶段总结与迁移主报告。

---

## 7. M4：扩展到 20 批并完成统计闭环

### 7.1 前置依赖

- 确认批次 11–20 的高/低保真数据完整；
- 新批次必须沿用既有采样分布、噪声协议和字段定义；
- 若外部数据尚未生成，M4 标记为“外部依赖阻塞”，但不得阻塞 M0–M3、M5。

### 7.2 实施动作

1. 新建 `migration/configs/heat_v3_qint_min_b20.yaml`，只将批次扩为 1–20；
2. 结果写入 `migration/results/heat_v3_qint_min_b20/`；
3. 以同批同测试集的 PWL–Physics-GP 差值作为主要配对统计量；
4. 主要终点为 @120 RMSE，次要终点为 12 档曲线、@10 小样本优势和 MSE/噪声；
5. 报告均值、标准差、95% CI、配对检验、效应量和 Holm 校正；
6. 在查看 11–20 批结果前固定统计方法和结论模板，避免事后改变口径。

### 7.3 终态表述规则

| 结果 | 允许表述 |
|---|---|
| PWL 均值更低且校正后显著 | “在当前协议下，PWL 对 Physics-GP 具有统计显著优势” |
| PWL 均值更低但不显著 | “PWL 保持均值优势趋势，但尚无充分显著性证据” |
| CI 跨越零且效应很小 | “PWL 与 Physics-GP 性能相当” |
| PWL 均值不再领先 | 撤回锚点 6 的优势表述，报告批次扩展后的真实结论 |

### 7.4 验收标准

- 20 批均有完整结果和质量检查；
- 主要终点、统计方法和多重校正可复核；
- 主报告、阶段总结和 PPT 使用同一终态表述；
- 不以“增加批次必然获得显著性”为前提解释结果。

---

## 8. M5：一键复现与 GitHub 交付检查

### 8.1 最小复现路径

从仓库根目录执行：

```powershell
uv sync --python 3.11 --extra dev
uv run pytest
uv run python reproduction/scripts/verify_sim_correctness.py
uv run python reproduction/run_reproduction.py `
  --config reproduction/configs/smoke.yaml `
  --output reproduction/results/smoke
uv run python migration/run_migration.py `
  --config migration/configs/heat_smoke.yaml `
  --output migration/results/heat_smoke_check
```

### 8.2 实施动作

1. 在 README 增加“当前结论”“最小复现”“完整实验”和“已知边界”入口；
2. 保留根级 `pyproject.toml` 与唯一 `uv.lock`，禁止子项目建立独立环境；
3. 增加 CI smoke：安装、pytest、独立正确性验证、两条业务线 smoke；
4. 检查 `.gitignore`，排除缓存、临时目录、joblib、重复运行结果和本地日志；
5. 当前数据与大结果只保留必要版本，其余移入 `legacy/` 或由生成说明替代；
6. 检查配置和报告是否含绝对路径、账号、令牌、机器名或其他本地信息；
7. 保证默认测试和运行不依赖任何 `legacy/` 文件；
8. 将阶段总结、本实施方案、主配置和必要数据说明纳入版本控制。

### 8.3 验收标准

- 全新环境按 README 可以完成安装、测试和两条 smoke；
- `uv run pytest` 与独立正确性验证全部通过；
- 当前主配置和结果均有来源说明；
- 无敏感信息、无本机绝对路径、无不必要的大型中间产物；
- GitHub 使用者能够区分论文原始内容、工程补全和场景创新。

---

## 9. 执行顺序与依赖

```text
M0 文档统一 ───────────────┐
                           ├─► M2 theta 判据修订
M1 v3 基线冻结 ────────────┤
                           ├─► M3 核心机制消融
                           └─► M4 20 批统计闭环（依赖批次 11–20）

M0–M4 结果收口 ─────────────► M5 一键复现与 GitHub 交付
```

推荐执行批次：

| 批次 | 工作 | 是否需要外部数据 |
|---|---|---|
| 第一批 | M0、M1 | 否 |
| 第二批 | M2、M3 smoke 与测试 | 否 |
| 第三批 | M3 完整消融 | 否 |
| 第四批 | M4 20 批扩展 | 是，批次 11–20 |
| 第五批 | M5 交付检查与结论收口 | 否 |

---

## 10. 条件性项目与非阻塞研究项

以下工作重要，但不应阻塞六项必须项目的执行：

| 项目 | 定位 | 进入条件 |
|---|---|---|
| 热传导 IV-B × 两规则 | minimum 适用边界验证 | M1 基线冻结后可并行 |
| 残余约 5 K² 分解 | 精度归因 | M3 机制消融完成后 |
| 标签节省分析 | 论文叙事补全 | M3 弱标签数量曲线完成后 |
| 低噪声敏感性 | 外部稳健性 | 新数据管线可用时 |
| refit / debias | v4 估计偏差升级 | 当前证据链收口后 |
| 联合 `g/d` 或变量投影 | v4 求解器升级 | 先确认无效候选和耦合是主要瓶颈 |
| rank-7 `H` / 等效热阻 | v4 可识别性升级 | M2 完成后单独立项 |
| 样本级物理权重 / Huber | v4 鲁棒性升级 | 热传导 IV-B 暴露失效边界后 |

严格论文数值复现不列为当前可执行的必须项，因为作者真实 `H/B`、输入协方差、
噪声和奇点处理规则仍未公开。除非获得作者材料，否则应继续使用“结构性复现”口径。

---

## 11. 完成定义

当前阶段只有在以下条件全部满足后才能标记为完成：

- [x] 当前报告对 v3、P3、minimum 和 `theta` 的表述一致（M0）；
- [x] v3 配置、数据、结果和环境已经冻结并可追溯（M1 基线卡）；
- [x] `theta` 的预测角色与物理解释已经重新定义（M2：nuisance 定案）；
- [x] 无 B、无弱标签、去机理列和弱标签数量消融均完成（M3）；
- [x] 20 批统计结果完成（M4：均值优势保持、Holm 0.071 未过校正门，按终态规则表述）；
- [ ] README、环境锁、测试、smoke 和发布检查能够支持独立复现（本地工作树与索引已一致；待 commit/push 后以远端 CI 完成最终验收）；
- [x] 阶段总结和对外 PPT 使用同一结论边界（M0–M4 回写同步）；
- [x] v4 研究候选未被误写成当前已经实现的功能。

> 阶段完成的最终标准不是“增加了多少功能”，而是当前 PWL 实例的配置可追溯、
> 机制可验证、统计结论可信、迁移边界明确，并能够由其他人在独立环境中复现。
