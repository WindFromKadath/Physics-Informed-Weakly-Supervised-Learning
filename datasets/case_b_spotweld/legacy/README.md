# 案例 B 数据历史归档

本目录仅用于数据来源溯源，不是复现实验的默认输入。

- `_raw/SAVE.tar.gz`：CRAN Archive 的 SAVE 1.0 源码包；
- `_raw/SAVE/`：解包后的 R 源码、文档和原始 `.rda` 数据。

当前可执行数据是上一级目录中的 `spotweldfield.csv` 与
`spotweldmodel.csv`。如需重新提取 CSV，应从这里的原始包开始，并与当前
CSV 做形状、列名和内容校验。
