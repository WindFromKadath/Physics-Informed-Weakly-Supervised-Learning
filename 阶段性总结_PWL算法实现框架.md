# PWL 项目阶段性总结：算法框架实现、迁移验证与后续升级

> 日期：2026-08-04
> 定位：阶段性总结文档。汇总仓库当前两条场景线（论文仿真复现 + COMSOL 热传导迁移）
> 的算法框架、配置谱系、选参规则、实验锚点、验证基础设施、迁移边界与后续升级路线
> 范围：`reproduction/`（仿真线与案例 B 线）、`migration/`（热传导迁移线）、
> `references/`（论文与解析文献）；57 项测试与 30 项独立数值检查全部通过

---

## 1. 总览

本仓库复现并扩展论文《Physics-Informed Weakly-Supervised Learning for Quality
Prediction of Manufacturing Processes》（Alenezi 等，IEEE T-ASE 2025）的 PWL 框架：

```
物理弱标签(免费)  +  少量监督标签  ──►  PWL 回归器  ──►  质量预测
   η(x;θ) 蒸馏          y               H(x;θ)g + B(x)d
```

**核心思想**：用物理模型 η 生成免费弱标签蒸馏物理形状（H 基、θ 校准参数），
用少量昂贵监督标签学习差异项（B 基、系数 d），稀疏组 Lasso 罚保证可辨识性。

**两条场景线**：

| 线 | 包 | 场景 | 当前状态 |
|---|---|---|---|
| 仿真线 | `reproduction/src/pwl_repro` | 论文 §IV 合成场景（IV-A/B/C） | 复现正确性已验证；IV-B M 档事故机制已定位 |
| 迁移线 | `migration/src/pwl_migration` | COMSOL 一维双层杆热传导 | **v3_qint_min 配置 12 档全优，均值通过锚点 6** |

### 1.1 本阶段统一定位

当前 PWL 应被定位为一套**可实例化的物理—数据联合学习算法框架**，而不是一套
可以不经修改直接部署到任意场景的固定模型。项目已经完成论文公开数学结构、
BCD–ADMM 优化骨架和实验基础设施的工程实现，但这不代表某一套固定的 `H/B`、
`theta` 和弱标签配置能够跨场景通用。

框架包含两个层次：

| 层次 | 内容 | 跨场景状态 |
|---|---|---|
| 通用算法内核 | `y = H(x_ph, theta)g + B(x_pr, x_ph)d`、监督/弱监督联合损失、稀疏组正则、BCD–ADMM、实验与质量检查 | 可复用 |
| 场景实例 | 物理模型、变量角色、`H/B` 基函数、`theta` 参数化、弱标签生成、物理可信度与选参规则 | 必须重新设计并验证 |

因此，“核心算法已经实现”与“PWL 只是算法框架”并不矛盾：前者描述代码与求解器
完成度，后者描述模型迁移边界。当前工程的价值是把新场景的主要工作从“重新实现
算法”转化为“正确完成物理问题实例化”。

---

## 2. 核心算法实现（`reproduction/src/pwl_repro`）

### 2.1 模型层 `model.py`

- **`PWLRegressor`**：主估计器。目标函数
  `½n⁻¹‖y − H(θ)g − Bd‖² + (λ_physics/2n_w)‖ỹ − H(θ)g‖² + λ_l1‖d‖₁ + λ_group·Σ‖θ组‖`
- **BCD 块坐标下降**：g / d / θ 三块交替，7 个近端算子（软阈值、组阈值、
  盒约束、岭投影等）全部闭式；
- **θ 处理模式**：联合更新（默认）/ 冻结（W2 审计对照）/ **刷新**（M1，
  弱标签随当前 θ 逐轮重估——案例 B 机制，热传导主配置采用）；
- **`calibrate_theta`**：式 (12) 约束最小二乘纯标定（对照诊断用）。

### 2.2 优化层 `optimization.py`

- **`consensus_admm`**：一致性 ADMM 求解器（θ 块），`soft_threshold` /
  `group_threshold` 闭式近端；`ConsensusResult` 记录迭代诊断。

### 2.3 特征层 `features.py`

- **`FeatureSpec`**（11 字段）：`name, x_ph_dim, x_pr_dim, theta_dim, h_names,
  b_names, groups, group_names, raw_h, raw_b, orthogonalize_b`；
- **`PWLFeatureLibrary`**：fit 时冻结标准化统计量（transform 可对任意 x 求值）；
  W1 新增 `orthogonalize_b`（B 对 H 列空间 Plumlee 残差化，投影矩阵 fit 冻结）；
  两项数值修复：**lstsq 列归一化**（防 rcond 截断吃掉小尺度方向）、
  **近零残差列置零**（防标准化把数值噪声放大成垃圾列）；
- **`get_feature_spec`**：仿真场景 H/B 库注册入口。

### 2.4 数据层 `simulation.py`

- 论文式 (9)–(11) 真模型：`eta_true`（物理模型）、`discrepancy`（差异项）、
  `process_contribution`、`saturation`；
- 噪声标定：按目标 SNR 解算 σ²；`toeplitz_covariance` 协方差结构；
- `generate_simulation`：嵌套划分（train/validation/test/弱标签/标定池）。

### 2.5 实验层 `experiments.py`

- **三大协议**：IV-A（标签量扫描 10–120，12 档）、IV-B（物理精度三档，
  弱标签 = η + c·δ 标定实现）、IV-C（标签节省）；
- **网格调参**：λ_physics × λ_l1 × λ_group 网格 + BCD/ADMM 候选并行评估
  （joblib loky），`require_convergence` 拒绝无效候选；
- **两种选参规则**（见 §4）；
- **`derive_label_savings_experiment`**：标签节省分析（配对单侧检验 + TOST）。

### 2.6 基线 `baselines.py`

Ridge / RF / GBDT / SVR / DT / GP（RBF+WhiteKernel）/ **Physics-GP**
（标定物理模型 + GP 残差，最强基线）/ **PhysicsDirect**（物理模型直接输出）。

### 2.7 案例 B 线 `case_b.py` / `case_b_experiments.py`

学位论文案例 B（Bayarri 数据，120 真实样本）复现；M1 刷新机制的实现源头。
（当前处于首轮完成状态，Table V 优势未复现，见《案例B复现报告》——
按用户指示暂不展开。）

---

## 3. COMSOL 迁移实现（`migration/src/pwl_migration`）

### 3.1 场景层 `heat.py`

- **场景物理**：一维双层杆 z∈[0,L]，L=0.1，界面 z=L/2，接触热阻
  R_c(P)=3e-2·P^(−0.7)（仅高保真），k(T)=k_true·(1+0.002(T−300))，
  k_true~U[15,35] 逐批；输出 y=界面两侧平均温度；低保真=闭式解
  （k_nominal=25，R_c=0，确定性无噪声）；
- **H 库**（11 列 4 组，**实际秩 7**）：[1,Q,Q² | 1/h,Q/h | T_inf,T_inf/h |
  θ,θQ,θQ²,θQ/h]——θ 组全为非仿射列的标量倍数，θ 不进标签拟合张成
  （θ 结构性不可识别的代数根源）；
- **B 库变体注册**（`get_heat_feature_spec`）：

| spec 名 | 列构成 | 出处/地位 |
|---|---|---|
| `heat` | 8 列 [1,P,1/P,P^(−1/2),Q/P,Q·P^(−1/2),Q,Q²] | 首版；Q/Q² 与 H θ 组重叠 |
| `heat_residualized` | 同列 + B 对 H 残差化 | W1 v1，**拒绝**（θ 全撞上界、小样本优势被摧毁） |
| `heat_no_qq` | 6 列（去 Q/Q²） | W1 v2，v3 前的基线 |
| **`heat_v3_qint`** | no_qq 6 列 + **q_int·P^(−0.7)** + **q_int/(1+hL/k_nom)·P^(−0.7)** | **当前主配置**；两列机理派生特征把参数类偏置 δ² 从 7.2 压到 0.44 K² |

- **`low_fidelity_interface_flux`**：q_int = QL/2 − k·a_LF（低保真闭式逐点
  免费计算，k=k_nominal=25）；第二列的 1/(1+hL/k_nom) 承载顶 BC 通量反馈
  极点的一阶结构——两列组合有效的数学原因。

### 3.2 实验层 `heat_experiments.py` / `cli.py`

- 复用 pwl_repro 的调参与评估设施，场景化锚点检查（`_heat_anchor_checks`，
  11 项 quality checks，见 §5.2）；
- `cli --scenario heat`：10 批 × 12 档 × 36 候选 default 规模（约 15 分钟）。

### 3.3 配置谱系（`migration/configs/`）

```text
heat_default（首版 8 列 B，1-SE）
  ├─ heat_v1_residualize  ❌ 拒绝
  ├─ heat_v2_no_qq        ≈ base，无重叠基线
  ├─ heat_theta_frozen    W2，与 v2 打平，审计对照
  ├─ heat_refresh         W10，全尺寸最优（@10 8.37），前主配置
  │    ├─ heat_v3_qint      +两列机理列，1-SE 臂：@120 5.607 仍输 0.077
  │    └─ heat_v3_qint_min  +两列机理列 + minimum 选参：✅ 12 档全优
  └─ （全部 selection_rule=physics_one_standard_error，唯 v3_qint_min 为 minimum）
```

---

## 4. 选参规则框架（双规则场景化定位）

| | `minimum` | `physics_one_standard_error`（1-SE） |
|---|---|---|
| 来源 | **论文原文**（§IV-A："the three tuning parameters are determined by achieving the lowest MSE on the validation set"） | 复现工程添加，代码标注 `[INFERRED]`，审计报告登记 |
| 机制 | λ̂ = argmin MSE_val | 阈值 = min MSE_val + SE（SE=std/√n_val），候选集内取最强 (λ_physics, λ_l1, λ_group) |
| 小样本行为 | 验证噪声直接兑换成测试误差（@10 差 1.47 RMSE） | 物理锚兜底，小样本稳定器 |
| 大样本行为 | λ 自然散落，无正则平移 | 选择集不收缩 ⇒ λ_physics 常驻 10，正则平移 b²≈3–6 K² |
| 适用场景 | 弱标签结构偏差大（热传导：PhysicsDirect 15.7≈3.5σ） | 弱标签近似无偏（仿真：实测相关 0.86–0.94） |

**决策（《选参规则跨场景对照报告》，P3）**：minimum **不推广**为全局主配置，
定位为场景相关规则——仿真线保持 1-SE，迁移线保持 minimum；操作化判据：
无训练基线直接精度 ≫3σ 优先 minimum，≲2σ 优先 1-SE，小网格双臂 smoke 定案。
附带否决：IV-B M 档事故**不是** 1-SE 协议伪影（两规则下均复现）。

---

## 5. 实验结果现状（锚点记分卡）

### 5.1 仿真线（`reproduction/results/paper_single/`，1-SE 主口径）

- **正确性**：静态审查 6 模块无实现错误；30 项独立数值验证全部通过
  （`scripts/verify_sim_correctness.py`）；57 项测试通过；
- **IV-A**：PWL MSE 11.67(10 标签)→4.77(120)，70 标签起 mse/noise≈1.02
  贴噪声下限（noise_var=4.659）；weak_r2 0.984（20 标签起）；
- **IV-B**：RMSE H/M/L = 1.969/3.506/2.010，M 档最差——机制重新定位为
  "标定落点 + 验证集分辨力双重夹击"（δ=7.76 已伤蒸馏但验证集不可分辨；
  L 档实测相关 0.858 远高于目标 0.50，λ=0.1 温和锚定反而最优）；
- **IV-C**：两臂均优于全部监督基线。

### 5.2 迁移线（`migration/results/heat_v3_qint_min/`，当前主配置）

| 锚点检查 | 状态 | 值 / 阈值 |
|---|---|---|
| 收敛率 / 无效候选率 | ✅ pass | 1.0 / 0.2435（≤0.25，贴限） |
| 小样本优势 | ✅ pass | gap@10=2.000 ≥ gap@120=0.307 |
| MSE/σ² @120 | ✅ pass | 1.428（≤2.0，迄今最接近噪声下限） |
| 优于 PhysicsDirect | ✅ pass | 5.366 vs 15.668 |
| **优于 Physics-GP（锚点 6）** | ✅ **pass（均值，10/20 批一致）** | **10 批 5.366 vs 5.530；20 批 5.351 vs 5.648；12 档全优；Holm 0.071 未过校正门** |
| d 系数物理一致性 | ℹ️ info | top-3 \|d\| 稳定含机理列（10/10 批） |
| 双保真相关性 | ❌ fail | [0.691, 0.854] 贴下界（数据属性） |
| θ 学习判定 | ℹ️ info（M2 判据修订） | 0.3（theta 定位 nuisance 参数，不再设门禁；无噪声等效标定 theta_eff ∈ [0.094, 0.131] 全部出盒，k_eff≈7.6–10.6） |
| θ 撞界率 | ℹ️ info（M2 判据修订） | 0.6（theta_eff 出盒的结构性后果，见《迁移后续工作/COMSOL场景theta判据修订.md》） |

**关键对照**（@120，10 批均值 RMSE）：PWL **5.366** < Physics-GP 5.530 <
GP 5.673 < 最优纯参数基线 6.330 ≪ PhysicsDirect 15.668。

**已知软肋（M4 终态，20 批）**：PWL<Physics-GP 配对单侧 p=0.036（未校正）、
Holm 0.071——**均值优势趋势保持且效应量增至中等（dz≈−0.43），但校正后仍不
显著**；批次符号覆盖 10/20，效应集中在大优势批次（批 15 −2.41 K、批 2 −1.53 K）。
见《迁移后续工作/COMSOL场景20批统计闭环报告》。

---

## 6. 归因与机制结论（本阶段最重要的认知成果）

PWL @120 超额误差 13.25 K²（refresh 基线）的双通道归因
（《归因分析 v2》，经独立复算验证）：

```
超额 13.25 ≈ δ²(≈7.2, B 库缺接触乘积方向 q_int·P^(−0.7), ~54%)
           + b²+est(≈6, 1-SE 正则平移 + 估计误差, ~46%)
```

- **基函数通道**：两列低保真免费机理列把 δ² 压到 0.44 K²；
- **协议通道**：minimum 选参解开 1-SE 钉死的 λ_physics=10；
- **缺一不可**：1-SE 臂 B 修复红利被强 λ 吸收大半（仅兑现 1.9 K²）——
  v3 通过 = "修好模型类" + "让训练真的逼近修好的类极限"。

---

## 7. 验证与质量基础设施

| 设施 | 位置 | 覆盖 |
|---|---|---|
| 单元/回归测试 | `reproduction/tests/`（7 文件）+ `migration/tests/test_heat.py` | **57 项全部通过**（含 M2 theta 信息级判据、M3 无 B/弱标签子采样、残差化正交性、θ 仿射与 prox 闭式对拍） |
| 仿真正确性独立验证 | `reproduction/scripts/verify_sim_correctness.py` | 30 项数值验证（prox/仿射/SNR/协议） |
| 归因复算脚本 | `migration/verify_attribution.py` | 高保真求解器 + 无噪声偏置分解（SVD 交叉验证） |
| 数据生成过程复现 | 分析脚本（本仓库会话） | 低保真 vs 工程预测 max\|err\|=5.7e-14；高保真 vs y_obs MSE=21.74≈σ²；温跃 vs dT_int 1.7e-11 |
| 锚点检查 | `heat_experiments._heat_anchor_checks` | 11 项 quality checks 随每次运行自动产出 |

---

## 8. 文档谱系

### 8.1 仿真线（`reproduction/reports/`）

《参考材料对照与复现改进总报告》（复现主报告，2026-07-28 快照，顶部有口径注）、
《论文内容与非论文修改代码审计》（非论文修改项登记）、《复现差值修改方案》
（已归档 `legacy/`）→《当前差值统一解释》（差值归因）、
《sensitivity_summary》（敏感性矩阵，`legacy/`）、《案例B实现审查报告》
+《案例B复现/》（案例 B 线）、《选参规则跨场景对照报告》（P3，规则决策）。

### 8.2 迁移线（`migration/reports/`）

**设计层**：《COMSOL三维热传导场景迁移分析》（场景设计、锚点出处）、
《COMSOL场景一维简化参考》（几何与闭式解，`legacy/`）、《COMSOL热传导场景与数据集
设计》、《COMSOL迁移检查清单》（`legacy/`）、《COMSOL场景灵敏度矛盾与设计决策》
（`legacy/`）；
**实验层**：《COMSOL场景PWL迁移实验报告》（首版，主报告，§0 已回写 v3 终态）+
`迁移后续工作/`：《迁移后续工作计划》（W1–W10，`legacy/迁移后续工作/`）、
《B库正交化实验报告》（W1，`legacy/迁移后续工作/`）、
《消融实验报告》（W2/W10 三臂，`legacy/迁移后续工作/`）、
《PWL大样本端归因分析》（v2 修正版，`legacy/迁移后续工作/`）、
《归因复算验证》（独立复算，`legacy/迁移后续工作/`）、
《v3机理列修复与协议消融实验报告》（实验证据）、
《v3唯一可行配置与通过机制》（配置唯一事实源）、
《theta判据修订》（M2，theta=nuisance 参数定案）、
**《v3通过后下一步计划》（当前行动纲领，P1–P9；P3 已完成）**。

---

## 9. PWL 模型演进与升级逻辑

当前框架的演进不是单纯的参数调优，而是从“公式可运行”逐步升级为“模型机制可
审计、迁移结果可归因”的过程：

| 代次 | 主要升级 | 解决的问题 | 阶段结果 |
|---|---|---|---|
| G0 论文骨架 | 联合模型、式 (5) 目标、BCD–ADMM | 给出通用数学框架 | 具体 `H/B` 等实现细节未公开 |
| G1 可执行复现 | 仿射 `H`、分组、紧凑 `B`、标准化与实验协议 | 将论文公式落成代码 | 结构可复现，绝对数值不能严格对齐 |
| G2 数值与协议修复 | 奇点、噪声标尺、ADMM/BCD 收敛、无效候选、统计制品 | 排除局部数值错误 | 形成可审计复现实验链 |
| G3 案例 B 审查 | 修复小样本 `B` 湮灭、`H/B` 完全重叠和验证泄漏 | 恢复差异补偿机制 | 机制修复，但论文优势仍受数据与协议未知项支配 |
| G4 热传导 v1 | 双保真数据、`theta=1/k`、`H11+B8` | 完成新物理场景首次实例化 | 小样本占优，大样本端失守，`theta` 撞界 |
| G5 热传导 v2 | 拒绝全残差化，采用 `no_qq`、refresh，完成风险分解 | 分离类偏置与协议/估计误差 | 定位缺失的接触乘积方向 |
| G6 热传导 v3 | 两列低保真机理列 + 场景化 minimum | 同时修复模型类与正则平移 | 12 档均值全优，@120 均值通过 |
| G7 建议升级 | refit、去偏、联合求解、有效秩 `H`、等效热阻与可信度权重 | 收紧残余误差、稳定性和解释性 | 尚未实施，属于下一阶段方案 |

v3 最重要的机制结论是：迁移成功不能依赖盲目扩展特征库，而要先用无噪声投影、
有效秩和误差分解确认模型类缺失方向，再加入低保真侧真实可获得的机理特征。当前
两列修复之所以有效，是因为它们直接表达了 `q_int * P^(-0.7)` 及其边界调制方向，
而不是因为增加了更多自由度。

下一代技术升级建议按以下顺序进行，且均应作为独立消融臂与 v3 基线比较：

1. 选定超参数后合并 train + validation 重新拟合；
2. 对稀疏支持集执行 active-set debias，降低 L1/组罚收缩偏置；
3. 使用 `d(g, theta)` 变量投影或联合 `g/d` 求解，减少 BCD 耦合；
4. 将 `H11` 压缩到有效秩 7，并保持物理分组与 `theta` 仿射语义；
5. 将不可识别的材料参数改写为等效热阻，或明确作为 nuisance parameter；
6. 引入样本级物理可信度或稳健弱标签损失；
7. 用 20 批配对实验确认改进是否具有统计稳定性。

以上 G7 内容是基于现有归因形成的升级建议，**不属于当前已完成实现**。

---

## 10. 新场景迁移的标准流程

PWL 迁移到其他实际场景时，应执行以下门禁，而不是直接复制热传导配置：

| 门禁 | 核心问题 | 主要验收内容 |
|---|---|---|
| Gate 0：问题定义 | 真实系统和低保真物理模型分别是什么 | 明确结构偏差，禁止只用同一输出叠加不同噪声冒充双保真 |
| Gate 1：变量划分 | 哪些是物理变量，哪些是额外过程变量 | `x_ph/x_pr` 角色清晰，测试时均可获得 |
| Gate 2：基函数设计 | `H` 蒸馏什么，`B` 补偿什么 | `H` 对 `theta` 仿射；`B` 包含遗漏机制且不依赖高保真标签 |
| Gate 3：可识别性 | 参数和补偿项是否混淆 | 检查有效秩、条件数、`H/B` 重叠、边界命中和参数学习分数 |
| Gate 4：模型类检查 | 当前特征库能否逼近高保真规律 | 用独立样本计算无噪声类偏置 `delta^2`，先证明方向再实施 |
| Gate 5：训练与消融 | 收益来自哪个机制 | 无 `B`、无弱标签、不同弱标签量、冻结/刷新和求解器消融 |
| Gate 6：统计确认 | 均值优势是否可靠 | 多批配对、置信区间、效应量、Holm 校正与噪声归一化 |
| Gate 7：文档收口 | 结论是否可复核 | 固定配置、数据版本、结果路径、已知差异和适用边界 |

迁移时需要重新确定的核心内容包括：物理模型、`x_ph/x_pr` 划分、`H/B`、
`theta`、映射 `phi`、弱标签偏差与噪声、物理知识可信度以及验证协议。任何新场景
都不应直接继承论文仿真或热传导场景的具体数值配置。

---

## 11. 统一对外说明与 PPT 汇报口径

### 11.1 推荐正式表述

> 本项目实现并验证了一套面向制造过程预测的物理知识学习（PWL）算法框架。
> 该框架利用 `H(x_ph, theta)g` 蒸馏低成本物理模型提供的规律，利用
> `B(x_pr, x_ph)d` 补偿物理模型未描述的过程效应与系统偏差，并通过监督损失、
> 物理弱监督损失和稀疏组正则进行联合训练。当前项目已经完成核心模型、优化器、
> 特征管理、实验协议和质量检查的工程实现，并在论文仿真、案例 B 和热传导场景
> 中进行了验证。PWL 的算法内核和迁移方法可以复用，但每个实际场景仍需依据物理
> 机制、变量结构、低保真偏差和参数可识别性重新设计并验证基函数与校准策略。

### 11.2 一句话介绍

> PWL 是一个将物理弱知识、真实数据和过程变量联合建模的可迁移算法框架：
> 求解器和实验流程可以复用，但每个实际场景都必须重新完成物理建模、基函数设计
> 和可识别性验证。

### 11.3 建议 PPT 结构（15–20 分钟）

| 页 | 结论式标题 | 主要内容 |
|---:|---|---|
| 1 | PWL 物理知识学习框架 | 标题、项目定位 |
| 2 | 当前成果是一套可迁移框架，而不是固定模型 | 结论先行：可复用部分与场景相关部分 |
| 3 | 数据模型和物理模型各有局限 | 少标记过拟合与低保真结构偏差 |
| 4 | PWL 用蒸馏与差异补偿统一两类信息 | `y = Hg + Bd` 核心框架图 |
| 5 | 稀疏联合训练决定模型使用多少物理知识 | 目标函数与 BCD–ADMM |
| 6 | 当前工程覆盖完整实验生命周期 | 核心、复现、迁移、测试与报告层 |
| 7 | 模型从论文公式演进为可审计迁移框架 | G0–G6 演进路线 |
| 8 | 论文结构可复现，但绝对数值不能严格复制 | 已实现内容与未公开设置 |
| 9 | 案例 B 证明基函数机制必须在真实数据上验证 | 两项错误、修复与结构性限制 |
| 10 | 热传导迁移重新设计了双保真关系 | 数据角色、变量与 v1 配置 |
| 11 | v1 到 v3 的升级针对模型类而非盲目调参 | residualized → no_qq → refresh → qint |
| 12 | 双通道修复使 v3 在均值层面通过 | 类偏置 7.2→0.44；@120 RMSE 对比 |
| 13 | 新场景必须通过完整迁移门禁 | Gate 0–7 |
| 14 | 20 批闭环：均值领先保持，显著性未过校正门 | M0–M4 收口结论（Holm 0.071）；G7/v4 展望 |
| 15 | PWL 的价值是提供可复用的场景建模方法 | 统一结论与适用边界 |

### 11.4 对外声明边界

可以明确说明：核心数学结构和优化器已经实现；工程框架可配置、可测试；热传导
v3 已取得 12 档均值全优；项目已经形成完整的场景迁移和归因方法。

目前不应声称：已严格复现论文全部数值；固定基函数适用于所有工业场景；PWL 已
普遍优于所有数据模型；`theta` 必然恢复真实物理参数；热传导优势已经统计显著。

---

## 12. 开放项与下一步（指向《v3通过后下一步计划》）

当前阶段的六项必须工作、执行顺序、交付物与完成定义，统一记录在
[《PWL 当前必须项目与实施方案》](PWL当前必须项目与实施方案.md)。

| 优先级 | 项 | 状态 |
|---|---|---|
| 阶段一 | ~~P1 主报告锚点回写~~ ✅ 已完成（M0：主报告 §0 回写 + 文档口径统一）；~~P2 W9 锚点 4 判据修订~~ ✅ 已完成（M2：theta 定位 nuisance 参数，判据改 info 级） | 已完成（2026-08-03） |
| 阶段二 | ~~P3 规则对照~~ ✅ 已完成（minimum 定位场景相关）；P4 热传导 IV-B × 两规则 | P4 待办（按 P3 修正验收口径：批内实测相关批均） |
| 阶段三 | P5 残余 ~5 K² 分解（待办）；~~P6 批次扩展 10→20~~ ✅ 已完成（M4：20 批均值优势保持、Holm 0.071 未过校正门） | P5 待办 |
| 阶段四 | ~~P7 v3 三项消融~~ ✅ 已完成（M3：四臂独立因果证据）；P8 标签节省；P9 sigma_low 档 | P8/P9 待办 |
| GitHub 交付 | M5 文档、环境锁、CLI、CI 与 smoke 已具备；Git 索引按最终目录收口 | 暂存完成后仍需 commit/push 与远端 CI 验证 |

---

## 13. 一句话总结

> 框架当前状态：**论文公开的 PWL 数学结构、BCD–ADMM 优化骨架和实验基础设施
> 已完成工程实现，并通过 57 项测试、30 项独立数值检查与两轮专项验证；COMSOL 迁移场景经“双通道
> 归因 → 两列机理列 + minimum 选参”修复后，PWL 在全部 12 个标签档取得均值
> 最优，并经四项机制消融获得独立因果证据（弱标签管小样本、B 管大样本、
> 机理列管反超、蒸馏收益 n_weak≈100 饱和）。20 批统计闭环：均值优势保持
> （@120 5.351 vs 5.648，dz≈−0.43，Holm 0.071），终态口径为“均值优势趋势、
> 尚无充分显著性证据”。当前成果应定位为一套可复用的 PWL 场景建模框架，
> 而不是跨场景即插即用的固定模型；新场景必须重新完成物理建模、基函数设计、
> 参数可识别性与统计验证。剩余工作为残余误差分解（P5）、标签节省（P8）、
> sigma_low 敏感性（P9）与 G7/v4 技术升级。**
